# ===========================================
# ChasCareer Environment Variables
# ===========================================
# Kopiera denna fil till .env och fyll i värdena

# ===========================================
# DATABASE (Supabase PostgreSQL)
# ===========================================
# Hittas i Supabase: Settings → Database → Connection string → URI
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-north-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection för migrations (utan pgbouncer)
# Hittas i Supabase: Settings → Database → Connection string → URI (Session mode)
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-north-1.pooler.supabase.com:5432/postgres"

# ===========================================
# NEXTAUTH / AUTH.JS
# ===========================================
# Generera med: openssl rand -base64 32
AUTH_SECRET="din-hemliga-nyckel-generera-med-openssl-rand-base64-32"

# URL till din app (localhost för development)
NEXTAUTH_URL="http://localhost:3000"

# ===========================================
# GOOGLE OAUTH
# ===========================================
# Skapa OAuth-credentials i Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ===========================================
# SUPABASE (valfritt - om du använder Supabase direkt)
# ===========================================
# Hittas i Supabase: Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"

# Hittas i Supabase: Settings → API → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Hittas i Supabase: Settings → API → service_role (HEMLIG - använd ej i frontend!)
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# ===========================================
# SLACK INTEGRATION (för notifikationer)
# ===========================================
# Skapa en Slack App på api.slack.com/apps
SLACK_BOT_TOKEN="xoxb-your-bot-token"
SLACK_SIGNING_SECRET="your-signing-secret"

# Webhook URL för att skicka meddelanden till en kanal
SLACK_WEBHOOK_URL="https://hooks.slack.com/services/YOUR_WEBHOOK_URL"

# ===========================================
# EMAIL (Resend - för påminnelser)
# ===========================================
# Skapa konto på resend.com och hämta API-nyckel
RESEND_API_KEY="re_123456789"

# Avsändaradress (måste verifieras i Resend)
EMAIL_FROM="noreply@chascareer.se"

# ===========================================
# ANALYTICS (valfritt)
# ===========================================
# PostHog
NEXT_PUBLIC_POSTHOG_KEY="phc_..."
NEXT_PUBLIC_POSTHOG_HOST="https://eu.posthog.com"

# ===========================================
# MONITORING (valfritt)
# ===========================================
# Sentry
SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"

# ===========================================
# APP CONFIG
# ===========================================
# Current environment
NODE_ENV="development"

# CRON secret för säkra cron-anrop (generera med: openssl rand -base64 32)
CRON_SECRET="your-cron-secret-here"

# App URL (för emails, etc)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
